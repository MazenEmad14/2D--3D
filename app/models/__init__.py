# app/models/__init__.py
# Models package — AI model weights, loaders, and inference wrappers live here.
# Future modules to add here:
#   loader.py       — Download / cache model weights at startup
#   detector.py     — Wall/opening detection inference (e.g. SegFormer, YOLOv8)
#   postprocess.py  — Convert raw model output to JSON IR entities
