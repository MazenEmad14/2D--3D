# app/routes/__init__.py
# Routes package — individual route modules live alongside this file.
# Future modules to add here:
#   upload.py    — POST /upload  (DXF / PDF / image ingestion)
#   convert.py   — POST /convert (trigger 3D generation)
#   models.py    — GET  /models  (list generated .glb files)
