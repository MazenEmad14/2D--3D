# tests/__init__.py
# Test suite root. Uses pytest.
# Future test modules:
#   test_health.py       — Verify /health and / endpoints
#   test_ir_schema.py    — Validate IR schema dataclasses
#   test_dxf_parser.py   — Unit tests for the DXF parser (Phase 1+)
#   test_pdf_parser.py   — Unit tests for the PDF parser (Phase 1+)
#   test_image_parser.py — Unit tests for the image parser (Phase 1+)
